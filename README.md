# demodeck----Word-to-PowerPoint-Converter
GUI tool that converts .docx files into .pptx presentations. Automatically maps document structure (H1 = title slide, H2 = section slides, bullets = content slides, tables = data slides) into clean, branded decks. Built for teams who spend too long copy-pasting content into slides.
